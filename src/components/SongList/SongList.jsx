
export const SongList = ({children}) => {
  return (
    <div className="container">
        <div className="d-flex flex-wrap justify-content-around">
            {children}
        </div>
    </div>
)
}
